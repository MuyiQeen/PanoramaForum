spring:
  application:
    name: demo
  banner:
    location: classpath:banner.txt

  datasource:
    url: jdbc:mysql://localhost:3306/pf?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 20061025
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      initialization-fail-timeout: 1



logging:
  level:
    com.zaxxer.hikari: DEBUG          # 连接池详情
    com.example.demo.dao.mapper: DEBUG # 你的 Mapper 执行的 SQL
    org.springframework.jdbc: DEBUG    # JDBC 相关



Panorama:
  version: 1.0.0
